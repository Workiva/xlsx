# Testing with Skynet - https://github.com/Workiva/skynet/tree/master/docs
name: xlsx-unit-tests
image: golang:1.18-bullseye
description: run xlsx unit tests
size: small
contact: support-onecloud

run:
  on-pull-request: true
  on-tag: true
  when-branch-name-is:
    - ^refs/tags/.+
    - master

scripts:
  - make test

artifacts:
  - /testing/out
